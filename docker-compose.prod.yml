# Surcharge prod — à utiliser avec : docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# Ou via : ./install-service.sh

services:
  frontend:
    build:
      context: ./frontend
      args:
        PUBLIC_URL_SERVEUR_PYTHON: http://192.168.1.31:8000
